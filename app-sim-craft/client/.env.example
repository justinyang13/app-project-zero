# Base URL of the SimCraft sync server (see ../sync-server). Optional — when
# unset the client uses the owner's own tailnet server (src/sync/syncClient.ts).
# Point it at a local copy of the server for development, e.g.:
# VITE_SYNC_SERVER_URL=http://localhost:4177
VITE_SYNC_SERVER_URL=
