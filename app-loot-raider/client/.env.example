# GraphQL API endpoint. Defaults to http://localhost:5183/graphql when unset.
# For the GitHub Pages build this is set via the RENDER_API_URL_LOOT_RAIDER
# repository variable in .github/workflows/deploy.yml, pointed at the
# Render-hosted API.
VITE_GRAPHQL_URL=http://localhost:5183/graphql
