namespace Application;

/// <summary>
/// Represents the intent to fetch every promotion (for the promotion
/// picker), not just the currently active one.
/// </summary>
public sealed record GetPromotionsQuery;
